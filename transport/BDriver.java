package transport;

public class BDriver extends Driver<PassengerCars> {
    public BDriver(String name, String licence, int experience, PassengerCars transport) {
            super(name, licence, experience, transport);
        }
}
